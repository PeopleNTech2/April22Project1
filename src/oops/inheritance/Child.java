package oops.inheritance;

public class Child extends Father{

    //Child is a subclass
    //Child cannot inherit from both Father and Mother classes
    //java doesn't support multiple inheritance
}
